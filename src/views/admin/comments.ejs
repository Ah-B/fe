<% layout('adminBoilerplate') -%>
    <section id="content">
        <section class="hbox stretch" ng-controller="adminCommentsController">
            <section>
                <section class="vbox">
                    <section class="scrollable padder-lg" id="bjax-target">
                        <br>
                        <section class="panel panel-default">
                            <header class="panel-heading">Comments</header>
                            <div class="table-responsive">
                                <table class="table table-striped b-t b-light">
                                    <thead>
                                        <tr>
                                            <th class="th-sortable" data-toggle="class">Id </th>
                                            <th>Comment</th>
                                            <th>Commenter</th>
                                            <th>Subject</th>
                                            <th>Remove</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr ng-repeat="comment in comments">
                                            <td>{{comment.comment._id}}</td>
                                            <td>{{comment.comment.text}}</td>
                                            <td>{{comment.comment.commenter.fName}} {{comment.comment.commenter.lName}}</td>
                                            <td>{{comment.subject}}</td>
                                            <td><a href="#" ng-click="removeComment(comment._id)" data-toggle="class"><i class="fa fa-times text-danger text"></i></a>
                                        </tr>
                                    </tbody>
                                </table>
                                <br>

                            </div>
                        </section>
                    </section>
                </section>
            </section>
        </section>
    </section>
